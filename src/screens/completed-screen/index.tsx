import React from 'react'
import { Text } from '../../utils/theme'
import SafeAreaWrapper from '../../components/shared/SafeAreaWrapper'

const CompletedScreen = () => {
  return (
    <SafeAreaWrapper>

    <Text variant='textXl'>HomeScreen</Text>
    </SafeAreaWrapper>  )
}

export default CompletedScreen